# ComplexNumberOperatorOverloading
A simple C++ program demonstrating operator overloading using a Complex number class.  It overloads the + operator to add two complex numbers and prints the result.
